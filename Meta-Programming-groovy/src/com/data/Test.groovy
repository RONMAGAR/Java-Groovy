package com.data


class Test1 {
    static void main(String[] args) {
//when we run Test1 main the groovy script won't run
    }
}
println("this is outside class and called as script bcz s we have created class " +
        "with different name from file name")//here compiler will explicitly add this into main method of test